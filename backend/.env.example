# Server port
PORT=5000

# MongoDB connection (replace with your URI)
MONGODB_URI=
MONGODB_DB_NAME=survly

# JWT secret (do NOT commit real secrets)
JWT_SECRET=

# Comma-separated allowed origins for CORS (e.g. https://yourdomain.com)
ALLOWED_ORIGINS=